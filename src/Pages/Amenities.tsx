import PageShell from '../components/PageShell/PageShell'

const Amenities = () => (
  <PageShell title="Amenities" subtitle="Everything the workspace offers, inside and out." />
)

export default Amenities
